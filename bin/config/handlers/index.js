const set = require('./setHandler');
const add = require('./addHandler');
const remove = require('./removeHandler');
const restore = require('./restoreHandler');

module.exports = {
  set,
  add,
  remove,
  restore,
};
